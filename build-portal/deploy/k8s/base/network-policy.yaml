apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: default-deny }
spec: { podSelector: {}, policyTypes: [Ingress, Egress] }
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: portal }
spec:
  podSelector: { matchLabels: { app: build-portal, component: web, architecture: sqlite } }
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - { ipBlock: { cidr: 192.168.22.0/24 } }
        - { ipBlock: { cidr: 10.244.0.0/16 } }
      ports: [{ protocol: TCP, port: 8443 }]
  egress:
    - to: [{ namespaceSelector: { matchLabels: { kubernetes.io/metadata.name: kube-system } } }]
      ports: [{ protocol: UDP, port: 53 }, { protocol: TCP, port: 53 }]
    - to: [{ ipBlock: { cidr: 10.155.208.112/32 } }]
      ports: [{ protocol: TCP, port: 80 }]
    - to: [{ ipBlock: { cidr: 0.0.0.0/0, except: [10.155.208.112/32] } }]
      ports: [{ protocol: TCP, port: 443 }]
